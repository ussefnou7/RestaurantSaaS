package com.smart.restaurant_saas.media.enums;

/** A stored rendition. {@code ORIGINAL} is the uploaded bytes, unmodified. */
public enum MediaVariantType {
    ORIGINAL,
    LARGE,
    MEDIUM,
    THUMB
}
