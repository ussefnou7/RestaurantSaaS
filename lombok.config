config.stopBubbling = true

# Allow Lombok-generated constructors (e.g. @RequiredArgsConstructor) to copy
# Spring's @Lazy onto the constructor parameter, so @Lazy constructor injection
# can be used to break dependency cycles.
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
